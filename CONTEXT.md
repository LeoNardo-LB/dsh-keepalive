# dsh-keepalive — 领域术语表（CONTEXT）

> dsh-keepalive 是 DSH 的提供商保活插件——本文件是项目领域术语表（glossary），只定义概念，不记录实现。

## Language

**提供商（provider）**:
DSH llm 服务里一个已配置的模型路由来源（如 deepseek-official），保活的作用对象。
_Avoid_: 模型（model 是提供商下的具体可选项，不是保活单位）

**保活（keepalive）**:
按抖动周期向提供商发送一条低成本拟人消息，以维持账号活跃特征的行为。
_Avoid_: 心跳（heartbeat，指连接层保活；本插件是账号层）

**发送/一发（shot）**:
一次保活动作的完整执行：构造消息 → ctx.llm.stream 读完 → 记录结果。
_Avoid_: 请求（request，指底层 HTTP 细节）

**基准间隔（base interval）**:
用户配置的周期基准值（分钟）；实际间隔在基准 ± jitter 内均匀取值。
_Avoid_: 周期（cycle，指实际发生的一次间隔）

**抖动（jitter）**:
相对基准间隔的百分比偏差范围，目的是让发送时刻不可预测。
_Avoid_: 随机偏差（口语表述，统一用 jitter）

**补发（catch-up）**:
进程重启/休眠后发现错过排定的发射时间时，立即补一发再按正常间隔续排的行为。
_Avoid_: 重试（retry，指失败后重发；补发针对错过时间而非失败）

**停放（parked）**:
某提供商因连续失败达到阈值被自动暂停保活的状态；需手动恢复。
_Avoid_: 禁用（disabled，指用户配置里手动关掉该提供商）

**HOST 半 / CLIENT 半**:
插件的两半：HOST 半跑在 dsh 进程内（引擎/路由/持久化），CLIENT 半跑在浏览器（slots UI）。
_Avoid_: 服务端/前端（DSH 语境统一用 host/client）

## 收录判据

| 收 | 不收 |
|----|------|
| 项目特有的领域概念 | 通用工程概念（超时/重试/工厂模式） |
| 多人/多会话反复使用且需统一的核心词 | 只出现一次的临时词 |
| 有混淆史的近义词对（用 Avoid 钉死） | 代码符号名（真相源是代码） |

## 维护纪律

新概念命名、发现近义词混用时当场沉淀进表；代码与用户表述矛盾时以表为准并更新表。
