# 架构债务登记簿（骨架）

> 已识别但暂不解决的技术债务。条目格式与状态流转见系统 templates/debt-entry.md；登记纪律见系统 workflows/debt.md。

| 字段速记 | 内容 |
|----------|------|
| 登记最少内容 | 标题 + 严重度（高/中/低） + 为什么是债 + 状态 |
| 状态词表 | 待处理 / 处理中 / 已消除 / 已接受 |
| 状态流转 | 待处理 → 处理中 → 已消除（附 commit）；待处理 → 已接受（必写理由 + 触发条件） |

## 架构违规

## 测试缺口

## 性能

## 遗留反模式

## 检查规则（grep 自动化，结果应为空）

```
# 依赖方向禁令：grep 禁止的 import 模式 <被检查目录> —— 待填
# 硬编码禁令：grep 硬编码模式 <被检查目录> —— 待填
```

## 维护记录

| 日期 | 动作（登记/偿还/复审） | 条目 |
|------|------------------------|------|